let myBook = {
    title: 'Ponniyin Selvan',
    pageCount: '2000',
    author: 'Kalki'
}

console.log (`The name of the book is ${myBook.title}, its has ${myBook.pageCount} pages and it was written by ${myBook.author}`)

let myPerson = {
    name: 'Andrew Meade',
    age: 26,
    location: 'Philadelphia'
}

console.log (`${myPerson.name} is ${myPerson.age} and he lives in ${myPerson.location}.`)

myPerson.age += 1
console.log (`${myPerson.name} is ${myPerson.age} and he lives in ${myPerson.location}.`)